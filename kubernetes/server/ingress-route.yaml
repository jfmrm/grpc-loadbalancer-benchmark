apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: server
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`server.10.245.79.157.nip.io`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: server-headless
          port: 50051
          kind: Service
          scheme: h2c
