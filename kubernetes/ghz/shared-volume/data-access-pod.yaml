apiVersion: v1
kind: Pod
metadata:
  name: dataaccess
spec:
  containers:
  - name: alpine
    image: alpine:latest
    command: ['sleep', 'infinity']
    volumeMounts:
    - name: ghz
      mountPath: /data
  volumes:
  - name: ghz
    persistentVolumeClaim:
      claimName: ghz
